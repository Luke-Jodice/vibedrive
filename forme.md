Next Steps
   1. API Keys: Copy .env.local.example to .env.local and add your Mapbox Access Token and
      Spotify Client ID/Secret.
   2. Spotify Dashboard: Ensure your Spotify app has
      http://localhost:3000/api/auth/callback/spotify as a valid redirect URI.
   3. Run Dev: Execute npm run dev to see the map and sidebar in action.